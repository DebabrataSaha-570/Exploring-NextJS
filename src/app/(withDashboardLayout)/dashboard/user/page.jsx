import React from "react";

export const metadata = {
  title: "ABC || User",
  description: "This is user page",
};

const UserPage = () => {
  return (
    <div>
      <h2>This is use page</h2>
    </div>
  );
};

export default UserPage;
